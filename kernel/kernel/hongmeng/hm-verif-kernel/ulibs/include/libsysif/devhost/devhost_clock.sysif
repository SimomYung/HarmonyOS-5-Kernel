/*
 * Copyright (C) Huawei Technologies Co., Ltd. 2020. All rights reserved.
 * Description: devhost clock operation sysif APIs
 * Author: Huawei OS Kernel Lab
 * Create: Sat Feb 29 16:11:40 2020
 */

/*
 * To support the mixed data model which means the client and the server
 * may use different data model. See the document for limitation of data type
 * used in the sysif interfaces in <Documentation/sysif_compat.md>.
 */
DEFINE_MANAGER(clock, 8,
	DEFINE_METHOD(clock, settime,
		      ARG(2,
			  unsigned long, tp,
			  unsigned long long, file_id),
		      RET_NO,
		      SLOWDATA_NO)
	DEFINE_METHOD(clock, gettime,
		      ARG(2,
			  unsigned long, tp,
			  unsigned long long, file_id),
		      RET_NO,
		      SLOWDATA_NO)
	DEFINE_METHOD(clock, getres,
		      ARG(2,
			  unsigned long, tp,
			  unsigned long long, file_id),
		      RET_NO,
		      SLOWDATA_NO)
	DEFINE_METHOD(clock, adjtime,
		      ARG(2,
			  unsigned long, tx,
			  unsigned long long, file_id),
		      RET_NO,
		      SLOWDATA_NO)
	DEFINE_METHOD(clock, poweroff_alarm,
		      ARG(1,
			  long, tm),
		      RET_NO,
		      SLOWDATA_NO)
)
