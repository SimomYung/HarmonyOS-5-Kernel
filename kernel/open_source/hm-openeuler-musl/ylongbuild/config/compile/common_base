
CONFIG_CAP_EXEC_ENV=

#
# define the architecture we compile for.
# CAP_ARCH values are the directories in mk/arch/
#
CONFIG_CAP_ARCH=

#
# machine can define specific variables or action for a specific board
# CAP_MACHINE values are the directories in mk/machine/
#
CONFIG_CAP_MACHINE=

#
# The compiler we use.
# CAP_TOOLCHAIN values are the directories in mk/toolchain/
#
CONFIG_CAP_TOOLCHAIN=

# CAP option as following
CONFIG_CAP_CACHELINE_SIZE=64
CONFIG_CAP_BIG_ENDIAN=n
CONFIG_CAP_LITTLE_ENDIAN=y
CONFIG_CAP_MAX_LCORE_NUM=4
CONFIG_CAP_MAX_MOD_NUM=40
CONFIG_CAP_MAX_WORK_NUM=32

# CAP Memory Management Config
CONFIG_CAP_MAX_BLK_MEMZONE=1024
CONFIG_CAP_HPE_RCU=y
CONFIG_CAP_HPE_TIMEWHEEL=n
CONFIG_CAP_HPE_MAX_TASKS=128

#LLT compile
CONFIG_CAP_LLT=n
#ASAN compile
CONFIG_CAP_ASAN=n

CONFIG_HPE_PAF=y
